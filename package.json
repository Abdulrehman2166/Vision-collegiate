{
  "name": "vision-collegiate",
  "version": "1.0.0",
  "description": "Vision Collegiate - root package, delegates to backend",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
