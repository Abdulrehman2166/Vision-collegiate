{
  "name": "vision-collegiate",
  "version": "1.0.0",
  "description": "Vision Collegiate monorepo - frontend (Next.js) + backend (Express)",
  "scripts": {
    "build:backend": "cd backend && npm install && npm run build",
    "start:backend": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
